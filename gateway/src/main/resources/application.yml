server:
  port: 8081
spring:
  application:
    name: spring-cloud-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: eureka-client-sale
          uri: lb://EUREKA-CLIENT-SALE
          predicates:
            - Path=/saleapi/**
          filters:
            - StripPrefix=1
# 增加前缀
#  prefix: /v1
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
